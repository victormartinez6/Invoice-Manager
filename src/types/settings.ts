export interface Settings {
  currency: string;
  paymentTerms: number;
  companyDetails: {
    name: string;
    email: string;
    phone: string;
    address: {
      street: string;
      city: string;
      state: string;
      zipCode: string;
      country: string;
    };
  };
}